# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Trained model files (generated by train.py).
# The base model and tokenizer are committed intentionally so users can
# run the PEFT demos without retraining (see README/CHANGELOG).
# 01-transformer-architecture/base_model.npz
# 01-transformer-architecture/base_tokenizer.json
